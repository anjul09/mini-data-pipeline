# Mini Data Pipeline

This is a small project to simulate a very basic data processing flow.

Right now it does three things:
- Validates incoming records
- Skips anything invalid
- Applies a simple transformation (doubling the value)

Nothing fancy as of now, just a starting point we can build on.